# Web / API
fastapi[standard]~=0.118.0   # включает fastapi-cli, uvicorn, watchfiles (можно заменить на твою строку fastapi~=0.118.0 + watchfiles)
uvicorn~=0.37.0
requests~=2.32.5
feedparser~=6.0.12

# HTML-парсинг и извлечение текста
beautifulsoup4~=4.12.3
lxml~=5.2.2                  # faster parser для bs4 (опционально, но сильно желательно)
trafilatura~=1.9.0           # извлечение основного текста статьи (опционально, но очень полезно)

# ML / NLP
torch~=2.8.0
transformers~=4.57.0
sentencepiece~=0.2.0         # обязательно для T5/ruT5 токенизатора
safetensors~=0.4.5           # обычно ставится вместе с transformers, но лучше явно

# (опционально) ускорение CPU/GPU
# accelerate~=0.34.0
# optimum~=1.21.0
